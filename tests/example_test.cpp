#include <gtest/gtest.h>

// A simple test case
TEST(BasicMathTest, Addition) {
    EXPECT_EQ(1 + 1, 2); // Test that 1 + 1 equals 2
}

TEST(BasicMathTest, Subtraction) {
    EXPECT_EQ(5 - 3, 2); // Test that 5 - 3 equals 2
}